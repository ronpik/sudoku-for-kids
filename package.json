{
  "name": "little-sudoku",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs --source",
    "prestart": "npm run build",
    "start": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/sudoku.js && node --check src/puzzle-worker.js && node --check scripts/build.mjs && node --check scripts/serve.mjs"
  }
}
